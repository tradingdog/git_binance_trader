安全说明：本文件不得保存任何明文密钥、账号、密码、Token。

请将真实凭据写入本地 `.env`（不入库），并仅在运行时通过环境变量加载：
- BINANCE_API_KEY
- BINANCE_API_SECRET
- FLY_API_TOKEN
- GITHUB_TOKEN

注意事项：
1. 本项目仅允许模拟资金交易，严禁实盘下单。
2. 用户提供的 API 仅用于行情、账户与系统相关能力，不得用于真实资金成交。
3. 若凭据曾被提交到历史记录，需立即轮换并作废旧凭据。